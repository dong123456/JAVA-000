<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="com.jike.week.dao.mapper.OrderMapper">	<resultMap type="com.jike.week.dao.po.OrderPO" id="order">		<id column="id" property="orderId"/>		<result column="order_no" property="orderNo"/>		<result column="user_id" property="userId"/>		<result column="order_amount" property="orderAmount"/>		<result column="shipping_amount" property="shippingAmount"/>		<result column="pay_amount" property="payAmount"/>		<result column="status" property="status"/>	</resultMap>	<select id="findOrderById" resultMap="order">		SELECT id,order_no,user_id,order_amount,shipping_amount,pay_amount FROM orders WHERE user_id = #{userId} and id = #{orderId} and status != -1	</select>	<select id="findOrderList" resultMap="order">		SELECT id,order_no,user_id,order_amount,shipping_amount,pay_amount FROM orders WHERE user_id = #{userId}  and status != -1 LIMIT  #{page},#{pageSize}	</select>	<insert id="insert" useGeneratedKeys="true" parameterType="com.jike.week.dao.po.OrderPO">		INSERT INTO orders (order_no,user_id,order_amount,shipping_amount,pay_amount)		VALUES (		#{orderNo},#{userId},#{orderAmount},#{shippingAmount},#{payAmount}		)	</insert>	<insert id="update" useGeneratedKeys="true" parameterType="com.jike.week.dao.po.OrderPO">		update orders set order_amount = #{orderAmount}, order_no = #{orderNo}, shipping_amount = #{shippingAmount}, pay_amount = #{payAmount}		 where user_id = #{userId} and id = #{orderId}	</insert>	<insert id="delete">		update orders set status = -1		where user_id = #{userId} and id = #{orderId}	</insert></mapper>